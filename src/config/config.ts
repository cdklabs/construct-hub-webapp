/**
 * Note: This file will be replaced at build-time when deployed from the construct-hub construct.
 */
import type { AppConfig } from "../types/config";

const config: AppConfig = {};

export default config;
